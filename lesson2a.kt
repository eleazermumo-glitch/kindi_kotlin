fun main(){
    //comaprision operators -they are used to to comapre more than one value hence the descion making during program exectution .the outcome of the comparisionn are  always boolean in nature(truwe.false)

    //logical operators AND=&& OR || NOT= ! -they are used where we need to test more than one condition at a go
    //AND - for outcome to be true both of the condittions outcome must turnout to be true
    //OR -for outcome to be true any of the condition must turn out to be true
    //NOT -it just negeates the outcome rg !true=false, !false=true

    //Conditional statements -if,if..else,if else if else
    //if -this one just test if a condition is met/true then a block of code is executed if false it terminates or procees with othe rlines of code

    //program to check positive numbers
    val number=-1
    if (number>0){
        //only is executed when true
        print("$number is a positive number")
    }
}